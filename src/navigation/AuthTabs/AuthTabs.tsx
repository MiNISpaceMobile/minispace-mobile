import { View } from "react-native";
import { Text } from "react-native-paper";

const AuthTabs = () => {
  return (
    <View>
      <Text>AuthTabs</Text>
    </View>
  );
};

export default AuthTabs;
